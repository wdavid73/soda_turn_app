/// Business and storage constants for SodaTurn.
class AppConstants {
  AppConstants._();

  /// Local storage key, compatible with the web v2 backup.
  static const String storageKey = 'almuerzo-turnos-v2';
}
